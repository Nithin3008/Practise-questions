export function Car({ arg1 }) {
    // console.log(data)

    return (
        <div>
            <h1>hi</h1>
            {/* <h1>{props.arg1}</h1>if we use props then this is the step */}
            <h2>{arg1.name}</h2>
        </div>
    )

}